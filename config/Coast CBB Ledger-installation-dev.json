{
  "confidential-port": 0,
  "auth-server-url": "${REACT_APP_AUTH_SERVER_URL}",
  "realm": "${REACT_APP_REALM}",
  "ssl-required": "${REACT_APP_SSL_REQUIRED}",
  "resource": "${REACT_APP_RESOURCE}",
  "credentials": {
    "secret": "${REACT_APP_SECRET}"
  }
}
